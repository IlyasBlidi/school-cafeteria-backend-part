spring:
  datasource:
    url: jdbc:mysql://localhost:3306/frew
    username: root
    password: ilyas#@2002
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    sql: debug

server:
  error:
    include-message: always
    include-binding-errors: always
